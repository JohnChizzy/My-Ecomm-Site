<!-- <% function getError(errors, prop) {
    try {
      return errors.mapped()[prop].msg;
    } catch (err) {
      return '';
    }
  }%> -->

  function getError(errors, prop) {
    if(errors){
      errors.forEach(error_msg => {
          return <%=error_msg[prop].msg%>;
        
      
        });

    }
     
      else{
        return '';
      }
  }